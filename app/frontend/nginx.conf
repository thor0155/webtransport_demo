server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;

    location = /app {
        return 301 /app/;
    }

    location /app/ {
        alias /usr/share/nginx/html/;
        index index.html;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}