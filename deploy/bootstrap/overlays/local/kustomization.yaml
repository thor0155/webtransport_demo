namespace: local

generatorOptions:
  disableNameSuffixHash: true
  
resources:
  - ../../base

patches:
  - path: patch-certificate.yaml
    target:
      kind: Certificate
      name: my-cert-keygen