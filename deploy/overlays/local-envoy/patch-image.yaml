apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
spec:
  template:
    spec:
      containers:
        - name: app
          image: demo-app:local
          imagePullPolicy: Always
          # imagePullPolicy: IfNotPresent
