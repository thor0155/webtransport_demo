apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  template:
    spec:
      containers:
        - name: api
          image: demo-backend:latest
          imagePullPolicy: Always
          # imagePullPolicy: IfNotPresent